#include <stdio.h>
#include "lists.h"
/**
 * fn - prints a message before the main function
 */
void fn(void)
{
char *str = "! and yet, you must allow,\nI bore my house upon my back!\n";

printf("You're beat%s", str);
}
